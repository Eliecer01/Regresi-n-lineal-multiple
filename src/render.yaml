# En la raíz de tu proyecto backend
echo 'services:
  - type: web
    name: regression-backend
    env: java
    plan: free
    buildCommand: mvn clean package
    startCommand: java -jar target/regresion-0.0.1-SNAPSHOT.jar
    envVars:
      - key: JAVA_VERSION
        value: 17' > render.yaml